name: Fix Analysis UI
on:
  workflow_dispatch:

# Intentionally manual only. Analysis is frozen on the Watchlist branch.
# Do not patch or rewrite Analysis from this workflow.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Analysis UI workflow is manual-only on this stable Watchlist branch."

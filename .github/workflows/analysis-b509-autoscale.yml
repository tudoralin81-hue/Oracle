name: B509 Analysis UI
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: echo 'B509 Analysis UI is superseded by B510 and is manual-only.'

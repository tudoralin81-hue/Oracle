name: Build Oracle Knowledge Link
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Legacy workflow notice
        run: echo 'Knowledge-link workflow is superseded by the current B510 build and is manual-only.'

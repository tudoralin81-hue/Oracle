name: Oracle APK B509 FINAL
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Legacy workflow notice
        run: echo 'B509 final workflow is superseded by B510 and is manual-only.'

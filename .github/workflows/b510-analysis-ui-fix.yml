name: B511 FINAL APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TEST_SECRET: ${{ secrets.ORACLE_KEYSTORE_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - name: Validate job environment
        run: |
          echo "Workflow job is valid"
          test -f app/build.gradle

name: B511 FINAL APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Baseline validation
        run: |
          echo "B511 workflow parsed successfully"
          test -f app/build.gradle
          test -f app/src/main/java/ro/alintudor/oracle/nativeui/OracleAnalysisModules.kt

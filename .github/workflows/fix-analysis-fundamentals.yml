name: Fix Analysis fundamentals

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Legacy workflow notice
        run: echo 'Analysis fundamentals repair is superseded by B510 and is manual-only.'
